import { CapacitorConfig } from '@capacitor/cli';

const APP_ID = process.env.TEKETEKE_APP_ID || 'com.teketeke.app';
const APP_NAME = process.env.TEKETEKE_APP_NAME || 'TekeTeke Go';
const WEB_DIR = process.env.TEKETEKE_WEB_DIR || 'web';

const config: CapacitorConfig = {
  appId: APP_ID,
  appName: APP_NAME,
  webDir: WEB_DIR,
  bundledWebRuntime: false,
  server: process.env.TEKETEKE_REMOTE_URL
    ? {
        url: process.env.TEKETEKE_REMOTE_URL,
        cleartext: false
      }
    : undefined,
  android: {
    backgroundColor: '#f6f7fb',
    allowMixedContent: true,
    useLegacyBridge: false
  },
  plugins: {
    SplashScreen: {
      launchAutoHide: true
    }
  }
};

export default config;
